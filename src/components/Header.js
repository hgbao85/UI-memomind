import React from 'react';
// import './App.css' // Nếu cần CSS riêng cho header

const Header = () => {
  return (
    <header className="app-header">
      <h1>Ứng Dụng Ghi Chú</h1>
    </header>
  );
};

export default Header;
